# Assets: 881 MB de JPGs. Decisao registrada no CLAUDE.md — nao versionar.
/assets

node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# SQLite local (o modo WAL cria tambem -wal e -shm). data/manifests/ VAI para o
# repositorio: sao 740 KB e o servidor precisa deles para montar o fichario.
/data/*.db
/data/*.db-wal
/data/*.db-shm

.env
.env*.local
.DS_Store
npm-debug.log*

# Skills de terceiros: restauráveis por `npx skills install` a partir do
# skills-lock.json, que é versionado.
/.agents/
/.claude/skills/
/.claude/settings.local.json

# Vinculo local com o projeto da Vercel (contem projectId e orgId da maquina).
.vercel
